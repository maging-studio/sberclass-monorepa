import React, { useState } from "react";
import styled, { css } from "styled-components";

export const ShadowClipWrapper = styled.div`
  position: relative;
  display: block;
  padding: 0 0 30px 0;
  margin-bottom: 80px;
  overflow: hidden;
`;
